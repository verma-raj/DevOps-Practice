apiVersion: v1
kind: Service
metadata:
  name: demo-svc
spec:
  selector: 
    app: 07Dec
  type: NodePort
  ports:
    - port: 80   # service port
      targetPort: 80 # container port
      nodePort: 30040  # Node port

